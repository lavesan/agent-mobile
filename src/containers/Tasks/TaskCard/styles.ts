import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cardContainer: {
    marginBottom: 16,
  },
  cardContainerActions: {
    flexDirection: "row",
    alignItems: "center",
  },
});
